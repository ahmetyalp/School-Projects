It is a simple project managment system written in Python using Django framework and uses Postgres SQL as database.

python manage.py runserver -> For run server<br>
python init.py -> For init Postgres Database
